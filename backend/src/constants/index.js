/**
 * =============================================================
 * SMARTFACTORY CONNECT - CONSTANTS INDEX
 * =============================================================
 * Central export for all constants used in the application
 */

const roles = require('./roles');

module.exports = {
  ...roles
};
